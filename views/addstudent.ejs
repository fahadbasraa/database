<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Student</title>
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff; /* Light blue background */
            color: #333; /* Darker text for readability */
        }

        h2 {
            text-align: center;
            color: #1565C0; /* Primary theme color */
            margin-top: 20px;
        }

        form {
            max-width: 400px;
            margin: 30px auto;
            padding: 20px;
            background: #ffffff; /* White background */
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            font-size: 14px;
            font-weight: bold;
            color: #06090d; /* Primary theme color */
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 14px;
        }

        input:focus {
            border-color: #1565C0; /* Primary theme color */
            outline: none;
            box-shadow: 0 0 5px rgba(21, 101, 192, 0.3);
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #1565C0; /* Primary theme color */
            color: #fff;
            font-size: 16px;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0d47a1; /* Darker shade of the theme color */
        }

        /* Responsive Design */
        @media (max-width: 500px) {
            form {
                width: 90%;
            }

            h2 {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <h2>Add Student</h2>
    <form action="/admin/add-student" method="POST">
        <!-- Roll Number -->
        <label for="rollNumber">Roll Number:</label>
        <input type="text" id="rollNumber" name="rollNumber" required>
        
        <!-- Password -->
        <label for="password">Password:</label>
        <input type="password" id="password" name="password">
        
        <!-- Full Name -->
        <label for="fullName">Full Name:</label>
        <input type="text" id="fullName" name="fullName" required>
        
        <!-- Grade -->
        <label for="grade">Grade:</label>
        <input type="text" id="grade" name="grade" required>
        
        <!-- Date of Birth -->
        <label for="dob">Date of Birth:</label>
        <input type="date" id="dob" name="dob" required>
        
        <!-- Parent's Name -->
        <label for="parentsName">Parent's Name:</label>
        <input type="text" id="parentsName" name="parentsName" required>
        
        <!-- Parent's Email -->
        <label for="parentEmail">Parent's Email:</label>
        <input type="email" id="parentEmail" name="parentEmail" required>
        
        <!-- Parent's Phone Number -->
        <label for="parentPhoneNumber">Parent's Phone Number:</label>
        <input type="text" id="parentPhoneNumber" name="parentPhoneNumber" required>
        
        <button type="submit">Add Student</button>
    </form>
</body>
</html>
