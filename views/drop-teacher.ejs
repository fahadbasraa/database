<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drop Teacher</title>
    <style>
        /* Add your styling here */
        .container {
            width: 80%;
            margin: 40px auto;
            background-color: white;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .error {
            color: red;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .success {
            color: green;
            font-weight: bold;
            margin-bottom: 20px;
        }

        input {
            padding: 10px;
            font-size: 1rem;
            margin-bottom: 20px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            background-color: #1565C0;
            color: white;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            border-radius: 5px;
        }

        button:hover {
            background-color: #1E88E5;
        }
    </style>
</head>
<body>

<header>
    <nav class="navbar">
        <!-- Navbar content here -->
    </nav>
</header>

<div class="container">
    <h2>Drop Teacher by Faculty ID</h2>
    
    <!-- Display success or error message -->
    <% if (err) { %>
        <p class="error"><%= err %></p>
    <% } %>

    <!-- Form to drop teacher -->
    <form action="/admin/drop-teacher" method="POST">
        <label for="facultyId">Enter Faculty ID to Drop:</label>
        <input type="text" id="facultyId" name="facultyId" required>
        <button type="submit">Drop Teacher</button>
    </form>
</div>
</body>
</html>
